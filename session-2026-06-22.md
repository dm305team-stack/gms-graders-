# Session 2026-06-22 — Prompts curados, Términos, fix de layout del reporte

Batch de features desplegadas a prod (`scanaeo.com`) tras el fix de grounding del
2026-06-18. Entrada principal: `session.md`. Prod HEAD: `cd2edef`.

---

## 1. Prompts curados estilo Peec (in-form) — `b172678` + `a6354bc`

El cliente puede definir hasta 5 prompts long-tail propios; el análisis corre sobre
esos + autogeneradas (hasta `AEO_MAX_QUERIES`), curados garantizados.

- **UI:** 5 cuadros editables **dentro del formulario de scope, debajo de "Product or service"** (no en un paso aparte; el primer intento fue 2-step y el operador lo rechazó, ver `a6354bc`). Opcionales: en blanco → autogeneramos. Botón **"Suggest prompts for me"** que los autocompleta vía el endpoint.
- **Endpoint nuevo:** `POST /api/suggest-queries` (scope → 5 long-tail, un Haiku call, sin motores). En `server/index.ts`. Mock devuelve sugerencias canned.
- **Backend:** `RunAnalysisInput.custom_queries?`, helper `generateQueries(input, {count, longtail})` extraído en `pipeline.ts`, modo `longtail` en `generate-queries.ts`, tipo `user_provided`. Stage A: `dedupe([...curated, ...autogen]).slice(0, maxQueries)`.
- **Filtro brand-free (marco AEO, ver memoria `aeo-scanner-framing`):** `parseCustomQueries` descarta en silencio cualquier prompt que contenga la marca o el dominio (reusa `brandVariants` de `@gms/llm`). Verificado: 2 prompts que nombraban la marca fueron filtrados.
- **Reporte:** las queries evaluadas aparecen en el reporte (requisito del operador), marcando las curadas (`you`).

## 2. Términos de Servicio — `8fdda38`

- Página estática `apps/aeo-auditor/public/terms.html` (servida en `/terms.html`), brand-matched (Fraunces/DM Sans/JetBrains, paleta cream/navy/rust).
- 4 bloques en inglés: **Data We Collect** (etiqueta de privacidad), **Use of Artificial Intelligence** (disclosure anti publicidad engañosa), **Binding Arbitration + Class Action Waiver**, **DMCA Copyright Policy** (texto verbatim del operador).
- Link **"Terms of Service"** en el footer del landing (`App.tsx` + `App.module.css`), abre en pestaña nueva.
- **Sin dirección física** (a pedido del operador): el agente DMCA queda contactable por teléfono + email. Nota: el safe harbor DMCA técnicamente pide dirección física; sirve un PO Box / registered agent, no tiene que ser la casa.

## 3. Fix de layout del reporte (texto pegado al borde inferior) — `cd2edef`

El operador reportó texto tocando el borde inferior en páginas 3, 4, 5 (las `.page`
están clavadas a 11in con `overflow:hidden`, el contenido largo se derrama sobre el
footer). En `server/report.ts`:

- Moví el bloque "The prompts we evaluated" de la pág 3 (Methodology, llena por la grilla 2x2) a la pág 4 (Scorecard, con espacio en blanco). Capeado a **6 prompts, una línea c/u** (truncados, `nowrap`/ellipsis) + "+N more". Los curados van primero.
- Pág 5 (Per-Engine): helper `shortenSource` trunca URLs largas (>34 chars) para que no envuelvan a 4 líneas y empujen la última fuente al borde.
- Verificado renderizando el sidecar REAL del reporte que subió el operador (`AEO-e2341ba4`, 12 prompts, 5 curados): páginas 3, 4, 5 dejan margen claro al footer.

**Trade-off:** los prompts en el reporte salen truncados a una línea y se muestran 6 + "+N more". Para mostrar los 12 completos haría falta una página dedicada (renumera páginas). Pendiente si el operador lo pide.

---

## Estado / proceso

- Todo desplegado a prod (`scanaeo.com`), contenedor healthy, health REAL ok.
- **Deploy sin preguntar:** el operador autorizó (2026-06-22) que deployee sin pedir confirmación cada vez. Flujo: commit → push → `ssh root@2.24.96.187` → `git pull` → `docker compose up -d --build`.
- Para verificar render del reporte sin correr un audit ($): sacar un sidecar (de `.aeo-data/reports/` o del contenedor con `docker exec`) y renderizar con un script tsx que llame `renderReportHtml` + `renderPdf`.

## Pendientes

1. **`PERPLEXITY_API_KEY` en el `.env` de PROD** (`/docker/gms-graders/apps/aeo-auditor/.env`, vacío) + rebuild. Entra el 4º motor.
2. Costo por audit ≈ $2.34 (12 queries × 3 motores grounded). Endpoint público. Bajar `AEO_MAX_QUERIES` si el volumen lo amerita.
3. (Opcional) Prompts completos en el reporte vía página dedicada.
4. Merge de `feature/aeo-pdf-report` a `main` (prod corre sobre la feature branch).
